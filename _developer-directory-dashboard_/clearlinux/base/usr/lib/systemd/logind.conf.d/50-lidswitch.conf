# see logind.conf(5) for more information
#
# configuration here will be overwritten by
# /usr/lib/systemd/logind.conf.d/80-lidswitch.conf
# which is installed with desktop bundles
# if you wish this file configuration to persist, you may run
#
# # mkdir -p /etc/systemd/logind.conf.d
# # ln -sf /dev/null /etc/systemd/logind.conf.d/80-lidswitch.conf
#
[Login]
HandleLidSwitch=ignore
